apiVersion: v1
kind: ConfigMap
metadata:
  name: zenoh-config
  namespace: robot-demo
  labels:
    app.kubernetes.io/name: robot-demo
data:
  zenoh-router.json5: |
    {
      mode: "router",
      listen: { endpoints: ["tcp/0.0.0.0:7447"] },
      scouting: {
        multicast: { enabled: false },
        gossip: { enabled: true }
      }
    }

  # Gazebo pod: routes /robot_N/* sensor topics from ros_gz_bridge.
  # Allow filter keeps only sensor+cmd_vel topics to avoid Nav2 announcement floods.
  zenoh-gazebo.json5: |
    {
      mode: "client",
      connect: { endpoints: ["tcp/zenoh-router:7447"] },
      scouting: { multicast: { enabled: false } },
      plugins: {
        ros2dds: {
          allow: "^((robot_1|robot_2)/(scan|odom|imu|joint_states|cmd_vel)|clock|ros_discovery_info|rosout)$"
        }
      }
    }

  # Nav2 pod (robot_1): allow only namespaced /robot_N/* topics so bare /map,
  # /tf etc. from other robots don't cross-pollinate local SLAM/Nav2.
  zenoh-nav2.json5: |
    {
      mode: "client",
      connect: { endpoints: ["tcp/zenoh-router:7447"] },
      scouting: { multicast: { enabled: false } },
      plugins: {
        ros2dds: {
          allow: "^((robot_1|robot_2)/(scan|odom|imu|joint_states|cmd_vel|tf|tf_static|map|plan|robot_description|goal_pose)|clock|ros_discovery_info|rosout)$"
        }
      }
    }

  zenoh-gazebo-robot-1.json5: |
    {
      mode: "client",
      connect: { endpoints: ["tcp/zenoh-router:7447"] },
      scouting: { multicast: { enabled: false } },
      plugins: {
        ros2dds: {
          allow: "^((robot_1|robot_2)/(scan|odom|imu|joint_states|cmd_vel)|clock|ros_discovery_info|rosout)$"
        }
      }
    }

  zenoh-gazebo-robot-2.json5: |
    {
      mode: "client",
      connect: { endpoints: ["tcp/zenoh-router:7447"] },
      scouting: { multicast: { enabled: false } },
      plugins: {
        ros2dds: {
          allow: "^((robot_1|robot_2)/(scan|odom|imu|joint_states|cmd_vel)|clock|ros_discovery_info|rosout)$"
        }
      }
    }

  zenoh-nav2-robot-1.json5: |
    {
      mode: "client",
      connect: { endpoints: ["tcp/zenoh-router:7447"] },
      scouting: { multicast: { enabled: false } },
      plugins: {
        ros2dds: {
          allow: "^((robot_1|robot_2)/(scan|odom|imu|joint_states|cmd_vel|tf|tf_static|map|plan|robot_description|goal_pose)|clock|ros_discovery_info|rosout)$"
        }
      }
    }

  zenoh-nav2-robot-2.json5: |
    {
      mode: "client",
      connect: { endpoints: ["tcp/zenoh-router:7447"] },
      scouting: { multicast: { enabled: false } },
      plugins: {
        ros2dds: {
          allow: "^((robot_1|robot_2)/(scan|odom|imu|joint_states|cmd_vel|tf|tf_static|map|plan|robot_description|goal_pose)|clock|ros_discovery_info|rosout)$"
        }
      }
    }

  # Viz pod: no allow filter — routes all /robot_N/* subscriptions from
  # tf_relay.py regardless of startup timing.
  zenoh-viz.json5: |
    {
      mode: "client",
      connect: { endpoints: ["tcp/zenoh-router:7447"] },
      scouting: { multicast: { enabled: false } }
    }
