# Robot 1 — patches the base deployments.
# The base deployment-nav2.yaml already sets ROBOT_NAME=robot_1 and uses
# zenoh-nav2.json5 (which has ros_namespace=/robot_1), so no nav2 patch needed.
# The gazebo-sim deployment uses zenoh-gazebo.json5 (no namespace) which is
# correct for the shared world — no patch needed there either.
# This file is intentionally minimal; left as a placeholder for future tweaks.
