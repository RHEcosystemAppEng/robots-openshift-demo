# Keep zenoh-router at 1 replica — multiple replicas cause routing isolation
# (clients on different routers can't share subscription interest).
# Single router ensures all Zenoh bridges share one routing table.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: zenoh-router
  namespace: robot-demo
spec:
  replicas: 1
